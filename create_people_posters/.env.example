#####################################
# Global notes
#####################################
# All scripts that use dotenv read only from: ./config/.env (next to each script).
# On PyInstaller builds, 'config' is created next to the executable.
# Booleans accept: true/false/1/0/yes/no (case-insensitive).
# Use absolute paths with forward slashes or double backslashes on Windows.

#####################################
# TMDB & Poster download (tmdb_people.py, prep_people_dirs.py)
#####################################
# REQUIRED: Your TMDB API key (v3 auth). Get from https://www.themoviedb.org/settings/api
TMDB_KEY=
# Optional: How deep to traverse person credits (0 = only direct person). Typical: 10
PERSON_DEPTH=10
# Optional: Path to a text file (one person name per line). Default: ./config/people_list.txt
PEOPLE_LIST=./config/people_list.txt
# Optional: Where to save downloaded posters. Default: ./config/posters
POSTER_DIR=./config/posters

#####################################
# People images repo & sync (sync_people_images.py, update_people_repos.py)
#####################################
# Optional: Root folder of the Kometa-People-Images repository (used by sync/update scripts). Default: ./Kometa-People-Images next to scripts
# ***NOTE*** You need to clone the 7 repos down and place them into a folder called Kometa-People-Images and this should NOT be inside the config
# folder for create_people_posters. Therefore you should definitely change the value below
PEOPLE_IMAGES_DIR=./Kometa-People-Images
# Optional: Git branch for Kometa-People-Images when updating. Default: master
PEOPLE_BRANCH=master

#####################################
# Adobe Express Remove Background automation (sel_remove_bg.py)
#####################################
# Chrome user-data folder to persist login (keep you signed in). Use an absolute path.
SEL_USER_DATA_DIR=./config/chrome-profile
# Chrome profile folder name (inside user-data dir). Usually 'Default' or 'Profile 1'
SEL_PROFILE_DIR=Profile 1

# Folder with input JPGs
SEL_SRC_DIR=./config/people_dirs/Downloads
# Where originals (pre-processed copies) should be stored
SEL_ORIG_DIR=./config/people_dirs/original
# Temp folder used by Chrome when the page downloads PNGs
SEL_DOWNLOAD_DIR=./config/sel_downloads

# Timing & flow
# Max seconds to wait for Adobe page to finish loading before processing
SEL_MAX_WAIT_READY_SEC=180
# Max seconds to wait for processing (download button appears)
SEL_PROC_TIMEOUT=120
# Max seconds to wait for file to appear in download folder after clicking download
SEL_MAX_WAIT_DL_SEC=20
# Seconds to keep trying to find the Download button
SEL_DL_BUTTON_TIMEOUT=12
# true/false: reload the page for each file (recommended true)
SEL_RELOAD_EACH_FILE=true

# Size enforcement
# Expected input width in pixels (e.g., 2000)
SEL_EXPECT_WIDTH=2000
# Expected input height in pixels (e.g., 3000)
SEL_EXPECT_HEIGHT=3000
# true/false: fail fast if the input image is not the expected size
SEL_ENFORCE_SIZE=true

# Advanced / optional
# URL of the Adobe Express Remove Background tool (override if it changes)
SEL_TOOL_URL=https://new.express.adobe.com/tools/remove-background
# Optional: comma-separated input extensions (defaults to jpg,jpeg)
# SEL_INPUT_EXTS=jpg,jpeg,webp
# true/false: enable pretty timing logs
